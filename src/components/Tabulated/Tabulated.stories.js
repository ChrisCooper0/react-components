import React from "react";
import Tabulated from "./Tabulated";

export default {
  title: "Components/Tabulated",
  component: Tabulated,
};

const Template = () => <Tabulated />;

export const Default = Template.bind({});
