import React from "react";
import LocalStorage from "./LocalStorage";

export default {
  title: "Components/LocalStorage",
  component: LocalStorage,
};

const Template = () => <LocalStorage />;

export const Default = Template.bind({});
