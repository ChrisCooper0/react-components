import React from "react";
import BackToTopButton from "./BackToTop";

export default {
  title: "Components/BackToTop",
  component: BackToTopButton,
};

const Template = () => <BackToTopButton />;

export const Default = Template.bind({});
