import React from "react";
import RatingBreakdown from "./RatingBreakdown";

export default {
  title: "Components/RatingBreakdown",
  component: RatingBreakdown,
};

const Template = () => <RatingBreakdown />;

export const Default = Template.bind({});
