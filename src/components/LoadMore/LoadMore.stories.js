import React from "react";
import LoadMore from "./LoadMore";

export default {
  title: "Components/LoadMore",
  component: LoadMore,
};

const Template = () => <LoadMore />;

export const Default = Template.bind({});
